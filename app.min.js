const express=require("express"),bodyParser=require("body-parser"),notesRoutes=require("./routes/notes"),dotenv=require("dotenv");dotenv.config();const app=express(),PORT=process.env.APP_PORT||3e3;app.use(bodyParser.json()),app.use("/api",notesRoutes),app.listen(PORT,(()=>{console.log(`Server is running on http://localhost:${PORT}`)}));